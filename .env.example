VITE_API_BASE_URL=/api/v1
VITE_BE_URL=https://tryly-dev-server.onrender.com

# Local dev (pnpm dev): VITE_BE_URL → vite proxy /api/* ไป backend
# Vercel Preview/Production: ตั้ง VITE_BE_URL ใน Dashboard → api/proxy.ts forward ไป BE
# ต้องใส่โดเมนเต็ม เช่น https://tryly-dev-server.onrender.com (มี https:// และ .com)
